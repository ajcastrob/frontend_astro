---
---

<style>
.hero {
  max-width: var(--max-width-hero);
  margin-inline: auto;
  padding: var(--space-128) var(--space-64) var(--space-64);
  text-align: center;
}
.hero__title {
  font-family: var(--font-display);
  font-size: var(--font-size-display);
  font-weight: 500;
  line-height: 1.1;
  letter-spacing: -0.02em;
  color: var(--color-text-primary);
  margin: 0 0 var(--space-32);
}
.hero__lead {
  margin: 0;
  font-size: var(--font-size-nav);
  line-height: 1.7;
  color: var(--color-text-secondary);
}
</style>

<section class="hero">
  <h1 class="hero__title">
    Dominando skills de IA: Del prompt a la automatización
  </h1>
  <p class="hero__lead">
    Un espacio para explorar herramientas de IA. Sin mucho ruido, a pasos con el desarrollo. 
  </p>
</section>
