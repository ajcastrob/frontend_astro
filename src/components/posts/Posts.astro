---
import SkillGrid from "@/components/posts/posts_grid/SkillGrid.astro"

const { posts, imageUrl, featured = false } = Astro.props
---

{posts.length === 0 ? (
  <p>Aún no hay skills publicadas.</p>
) : (
  <SkillGrid posts={ posts } imageUrl={ imageUrl } featured={ featured }/>
)}

